# SMTP settings for Nodemailer (use a provider like Mailtrap for staging)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password

# Where contact form submissions should land
CONTACT_RECIPIENT=advisory@ptsfinancialservices.in
